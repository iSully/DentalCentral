{% extends 'base.html.twig' %}

{% block body %}
    <script>
        {% if user != null %}
           window.location = "/dashboard";
        {% else %}
       window.location = "/login";
        {% endif %}
    </script>
{% endblock %}

